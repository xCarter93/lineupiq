---
phase: 19-ensemble-models
plan: 03
type: execute
wave: 2
depends_on: ["19-01", "19-02"]
files_modified: [packages/backend/src/lineupiq/models/ensemble.py, .planning/phases/19-ensemble-models/BENCHMARK_RESULTS.md]
autonomous: true
---

<objective>
Benchmark ensemble strategies against single models on 2024 holdout data.

Purpose: Determine empirically whether ensembles improve accuracy over best single model. Evidence-based decision making, not assumptions.
Output: BENCHMARK_RESULTS.md showing MAE/R² for all strategies across all positions, with clear winner identified.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-ensemble-models/19-RESEARCH.md
@.planning/phases/19-ensemble-models/19-CONTEXT.md
@.planning/phases/13-kdef-models/13-08-SUMMARY.md
@packages/backend/src/lineupiq/models/ensemble.py
@packages/backend/src/lineupiq/models/backtesting.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add benchmark_ensemble_strategies function to ensemble.py</name>
  <files>packages/backend/src/lineupiq/models/ensemble.py</files>
  <action>Add benchmark_ensemble_strategies(position, stat, X_train, y_train, X_holdout, y_holdout) function that: (1) Loads LightGBM and XGBoost models for position/stat; (2) Evaluates 5 strategies on holdout: lgbm_solo, xgb_solo, voting_simple, voting_weighted (optimal weights via grid search 0-1 in 0.05 steps), stacking (Ridge meta-learner, cv=5); (3) Returns dict with MAE and R² for each strategy; (4) Identifies best_strategy by lowest MAE. Use sklearn metrics: mean_absolute_error, r2_score. Follow research pattern from 19-RESEARCH.md Example 1.</action>
  <verify>Function signature correct, returns dict with strategy names as keys</verify>
  <done>Benchmark function ready to evaluate all strategies</done>
</task>

<task type="auto">
  <name>Task 2: Run benchmark on QB passing_yards (pilot)</name>
  <files>None (output to console/logs)</files>
  <action>Use backtesting.py to split 2024 season as holdout. Run benchmark_ensemble_strategies for QB passing_yards. Print results showing MAE/R² for all 5 strategies. Verify weighted grid search finds optimal weights. Check correlation between LightGBM and XGBoost predictions (research says correlation < 0.9 indicates good diversity). This is pilot to validate benchmark before running all positions.</action>
  <verify>Console output shows 5 strategies with MAE/R² metrics and best_strategy identified</verify>
  <done>Pilot benchmark confirms methodology works</done>
</task>

<task type="auto">
  <name>Task 3: Benchmark all skill position stats</name>
  <files>.planning/phases/19-ensemble-models/BENCHMARK_RESULTS.md</files>
  <action>Run benchmark_ensemble_strategies for all 24 skill position stats (6 QB + 7 RB + 4 WR + 4 TE). Create BENCHMARK_RESULTS.md with: (1) Summary table showing best strategy for each stat; (2) Full results table with all 5 strategies per stat; (3) Key findings section analyzing: diversity (avg correlation between base models), improvement (% of stats where ensemble beats best single model), winning strategy (most common best performer). (4) Recommendation based on data: adopt ensemble if it wins on majority of stats, keep single model otherwise. Use Phase 13-08 backtesting approach for consistent holdout validation.</action>
  <verify>BENCHMARK_RESULTS.md exists with summary, full results, and recommendation</verify>
  <done>Complete benchmark results documented, clear data-driven recommendation</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] benchmark_ensemble_strategies function exists
- [ ] Pilot run on QB passing_yards shows all 5 strategies
- [ ] BENCHMARK_RESULTS.md has results for all 24 stats
- [ ] Clear recommendation (adopt ensemble or keep single model)
</verification>

<success_criteria>

- All tasks completed
- Benchmark methodology validated on pilot stat
- All skill position stats benchmarked on 2024 holdout
- BENCHMARK_RESULTS.md provides evidence-based recommendation
- User knows whether to proceed with ensemble integration
  </success_criteria>

<output>
After completion, create `.planning/phases/19-ensemble-models/19-03-SUMMARY.md`
</output>
