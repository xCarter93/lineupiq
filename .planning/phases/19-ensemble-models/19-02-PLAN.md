---
phase: 19-ensemble-models
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [packages/backend/src/lineupiq/models/ensemble.py, packages/backend/src/lineupiq/models/__init__.py]
autonomous: true
---

<objective>
Create ensemble infrastructure for combining LightGBM + XGBoost models.

Purpose: Provide VotingRegressor and StackingRegressor wrappers for three ensemble strategies: simple averaging, weighted averaging, and stacking with meta-learner.
Output: ensemble.py module with functions to create, fit, and save ensemble models.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-ensemble-models/19-RESEARCH.md
@packages/backend/src/lineupiq/models/persistence.py
@packages/backend/src/lineupiq/models/training.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ensemble.py module</name>
  <files>packages/backend/src/lineupiq/models/ensemble.py</files>
  <action>Create ensemble.py with: (1) create_voting_ensemble(lgbm_model, xgb_model, weights=None) -> VotingRegressor - simple if weights=None, weighted if weights provided; (2) create_stacking_ensemble(lgbm_model, xgb_model, cv=5) -> StackingRegressor with Ridge(alpha=1.0) meta-learner; (3) save_ensemble(ensemble, position, stat, ensemble_type) -> saves to packages/backend/models/{position}_{stat}_{ensemble_type}.joblib; (4) load_ensemble(position, stat, ensemble_type) -> loads ensemble model. Include docstrings following project pattern from training.py.</action>
  <verify>ensemble.py exists with 4 functions: create_voting_ensemble, create_stacking_ensemble, save_ensemble, load_ensemble</verify>
  <done>Ensemble infrastructure ready for use</done>
</task>

<task type="auto">
  <name>Task 2: Add ensemble module to __init__.py exports</name>
  <files>packages/backend/src/lineupiq/models/__init__.py</files>
  <action>Import ensemble functions in __init__.py for clean API: from lineupiq.models.ensemble import create_voting_ensemble, create_stacking_ensemble, save_ensemble, load_ensemble. Follow existing pattern in __init__.py.</action>
  <verify>Ensemble functions importable from lineupiq.models</verify>
  <done>Ensemble module accessible via clean import path</done>
</task>

<task type="auto">
  <name>Task 3: Write unit tests for ensemble module</name>
  <files>packages/backend/tests/test_ensemble.py</files>
  <action>Create test_ensemble.py with: (1) test_create_voting_simple() - simple averaging works; (2) test_create_voting_weighted() - weighted averaging respects weights; (3) test_create_stacking() - stacking with Ridge meta-learner works; (4) test_save_load_ensemble() - persistence round-trip preserves predictions. Use mock models (sklearn DummyRegressor) to keep tests fast. Follow pytest pattern from existing tests.</action>
  <verify>uv run pytest tests/test_ensemble.py passes</verify>
  <done>Ensemble module validated with unit tests</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ensemble.py exists with 4 core functions
- [ ] Functions importable from lineupiq.models
- [ ] pytest tests/test_ensemble.py passes
- [ ] Docstrings follow project style
</verification>

<success_criteria>

- All tasks completed
- Ensemble infrastructure ready for benchmarking
- Unit tests prove functions work correctly
- Clean API for creating/saving/loading ensembles
  </success_criteria>

<output>
After completion, create `.planning/phases/19-ensemble-models/19-02-SUMMARY.md`
</output>
