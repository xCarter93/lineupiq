---
phase: 19-ensemble-models
plan: 04
type: execute
wave: 3
depends_on: ["19-03"]
files_modified: [packages/backend/src/lineupiq/api/predict.py, packages/backend/src/lineupiq/models/ensemble.py, packages/backend/models/*_ensemble.joblib]
autonomous: true
---

<objective>
Integrate winning ensemble strategy into prediction API (if ensembles won benchmarking).

Purpose: Deploy the best-performing model strategy to production API. If single models won, document why and keep current API unchanged.
Output: Updated prediction API using ensemble models (if adopted), or documented decision to keep single models.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-ensemble-models/19-RESEARCH.md
@.planning/phases/19-ensemble-models/19-CONTEXT.md
@.planning/phases/19-ensemble-models/BENCHMARK_RESULTS.md
@packages/backend/src/lineupiq/api/predict.py
@packages/backend/src/lineupiq/models/ensemble.py
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Choose integration strategy based on BENCHMARK_RESULTS.md</decision>
  <context>Benchmark results in 19-03 show whether ensembles improve accuracy. Three possible outcomes: (A) Ensemble wins on majority of stats → integrate ensemble; (B) Single models win → keep current API; (C) Mixed results → position-specific decision.</context>
  <options>
    <option id="integrate-ensemble">
      <name>Integrate ensemble as default</name>
      <pros>Improved accuracy on holdout data, better predictions for users</pros>
      <cons>Slightly higher inference latency (2 models + meta-learner for stacking)</cons>
    </option>
    <option id="keep-single">
      <name>Keep single models (LightGBM or XGBoost)</name>
      <pros>Simpler, faster inference, already validated</pros>
      <cons>Potentially lower accuracy if ensemble showed improvement</cons>
    </option>
    <option id="position-specific">
      <name>Use ensemble for some positions, single for others</name>
      <pros>Optimizes per-position, uses best strategy for each stat</pros>
      <cons>More complex API logic, harder to maintain</cons>
    </option>
  </options>
  <resume-signal>Select integration strategy: integrate-ensemble, keep-single, or position-specific (based on BENCHMARK_RESULTS.md findings)</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Train and save winning ensemble models (if ensembles adopted)</name>
  <files>packages/backend/models/*_ensemble.joblib</files>
  <action>If integrate-ensemble or position-specific selected: For each stat where ensemble wins, train the best ensemble strategy (voting_simple, voting_weighted, or stacking) on full training data (not just holdout) and save as {position}_{stat}_ensemble.joblib. Use ensemble.save_ensemble() function. If weighted voting wins, use optimal weights from benchmarking. If stacking wins, use Ridge(alpha=1.0) with cv=5. Skip if keep-single selected.</action>
  <verify>Ensemble model files exist for all stats where ensemble was chosen</verify>
  <done>Ensemble models trained on full data and persisted</done>
</task>

<task type="auto">
  <name>Task 2: Update prediction API to use ensemble models</name>
  <files>packages/backend/src/lineupiq/api/predict.py</files>
  <action>If integrate-ensemble: Update load_models() function to prefer *_ensemble.joblib over *_lgbm.joblib. If position-specific: Add logic to check for ensemble model first, fallback to single model if ensemble doesn't exist. Update predict_player_stats() to use ensemble.predict() (same sklearn API as single models). If keep-single: Add comment documenting why ensembles were not adopted (reference BENCHMARK_RESULTS.md). No code changes needed for keep-single.</action>
  <verify>API loads and uses correct model type based on decision</verify>
  <done>Prediction API updated to use optimal model strategy</done>
</task>

<task type="auto">
  <name>Task 3: Validate API predictions with ensemble</name>
  <files>None (curl tests)</files>
  <action>If ensembles adopted: Start FastAPI server (uv run uvicorn lineupiq.api.main:app). Test predictions for one player per position (QB, RB, WR, TE) using curl. Verify predictions return successfully, values are reasonable (not NaN, within expected ranges). Compare prediction values to single model baseline to ensure ensemble is actually being used. If keep-single: Verify existing API still works with single models.</action>
  <verify>curl tests return valid predictions, ensemble models loaded correctly</verify>
  <done>API integration validated, predictions working</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Decision made based on BENCHMARK_RESULTS.md
- [ ] Ensemble models trained and saved (if adopted)
- [ ] API updated to use chosen strategy
- [ ] curl tests confirm predictions work
</verification>

<success_criteria>

- All tasks completed
- Integration decision documented and implemented
- API uses best-performing model strategy
- Predictions validated via curl tests
- Phase complete: ensemble models deployed OR decision documented to keep single models
  </success_criteria>

<output>
After completion, create `.planning/phases/19-ensemble-models/19-04-SUMMARY.md`
</output>
