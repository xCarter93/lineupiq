---
phase: 19-ensemble-models
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [packages/backend/src/lineupiq/models/qb.py, packages/backend/src/lineupiq/models/rb.py, packages/backend/src/lineupiq/models/receiver.py, packages/backend/models/*_xgb.joblib]
autonomous: true
---

<objective>
Train XGBoost models for all skill positions to complement existing LightGBM models.

Purpose: Provide model diversity for ensemble methods. LightGBM (leaf-wise growth) + XGBoost (level-wise growth) have complementary strengths.
Output: XGBoost models for QB/RB/WR/TE stats saved as {position}_{stat}_xgb.joblib files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-ensemble-models/19-RESEARCH.md
@.planning/phases/13-kdef-models/13-01-SUMMARY.md
@packages/backend/src/lineupiq/models/training.py
@packages/backend/src/lineupiq/models/qb.py
@packages/backend/src/lineupiq/models/rb.py
@packages/backend/src/lineupiq/models/receiver.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add XGBoost training function to qb.py</name>
  <files>packages/backend/src/lineupiq/models/qb.py</files>
  <action>Add train_qb_models_xgboost() function that mirrors train_qb_models() but uses model_type="xgboost" and saves with "_xgb.joblib" suffix. Use same 30 Optuna trials as LightGBM (Phase 13-01 decision). Function should train all 6 QB targets: passing_yards, passing_tds, interceptions, rushing_yards, rushing_tds, fumbles_lost.</action>
  <verify>Function exists with correct signature and docstring</verify>
  <done>train_qb_models_xgboost() callable, saves models with _xgb suffix</done>
</task>

<task type="auto">
  <name>Task 2: Add XGBoost training function to rb.py</name>
  <files>packages/backend/src/lineupiq/models/rb.py</files>
  <action>Add train_rb_models_xgboost() function that mirrors train_rb_models() but uses model_type="xgboost". Train all 7 RB targets: rushing_yards, rushing_tds, carries, receiving_yards, receiving_tds, receptions, fumbles_lost. Use 30 Optuna trials.</action>
  <verify>Function exists with correct signature</verify>
  <done>train_rb_models_xgboost() callable, saves models with _xgb suffix</done>
</task>

<task type="auto">
  <name>Task 3: Add XGBoost training functions to receiver.py</name>
  <files>packages/backend/src/lineupiq/models/receiver.py</files>
  <action>Add train_wr_models_xgboost() and train_te_models_xgboost() functions that mirror existing train_wr_models() and train_te_models() but use model_type="xgboost". Train 4 targets each: receiving_yards, receiving_tds, receptions, fumbles_lost. Use 30 Optuna trials.</action>
  <verify>Both functions exist with correct signatures</verify>
  <done>WR and TE XGBoost functions callable, save models with _xgb suffix</done>
</task>

<task type="auto">
  <name>Task 4: Train all XGBoost models</name>
  <files>packages/backend/models/*_xgb.joblib</files>
  <action>Run all XGBoost training functions: train_qb_models_xgboost(), train_rb_models_xgboost(), train_wr_models_xgboost(), train_te_models_xgboost(). This creates 24 XGBoost model files (6 QB + 7 RB + 4 WR + 4 TE + 3 for consistency with LightGBM). Use uv run python -c "from lineupiq.models.qb import train_qb_models_xgboost; ..." pattern.</action>
  <verify>ls packages/backend/models/*_xgb.joblib shows 24 XGBoost model files</verify>
  <done>All skill position XGBoost models trained and saved</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] 24 *_xgb.joblib files exist in packages/backend/models/
- [ ] Each position module has train_*_models_xgboost() function
- [ ] Models can be loaded with joblib.load()
</verification>

<success_criteria>

- All tasks completed
- XGBoost models exist for QB/RB/WR/TE (24 total)
- Models saved with _xgboost.joblib suffix for clear distinction
- Training functions reusable for future retraining
  </success_criteria>

<output>
After completion, create `.planning/phases/19-ensemble-models/19-01-SUMMARY.md`
</output>
