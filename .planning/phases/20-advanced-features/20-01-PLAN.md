---
phase: 20-advanced-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/src/lineupiq/data/weather_cache.py
  - packages/backend/src/lineupiq/features/weather.py
  - packages/backend/src/lineupiq/features/pipeline.py
  - packages/backend/tests/test_weather_features.py
  - packages/backend/pyproject.toml
autonomous: true
domain: ml-features
user_setup:
  - service: visual-crossing
    why: "Historical weather data for NFL game conditions"
    env_vars:
      - name: VISUAL_CROSSING_API_KEY
        source: "https://www.visualcrossing.com/account → Create free account → API Keys section"
    account_setup:
      - url: "https://www.visualcrossing.com/sign-up"
        skip_if: "Already have Visual Crossing account"
    local_dev:
      - "Free tier: 1,000 records/day (sufficient for full 2022-2025 training data)"
---

<objective>
Expand weather features from basic temp/wind to detailed conditions including temperature bins, wind thresholds, precipitation, and dome/outdoor encoding.

Purpose: Improve model accuracy for weather-sensitive positions (QB, WR) by adding research-backed weather thresholds (freezing <32°F, high wind >15mph, precipitation impact).
Output: Enhanced weather feature module with Visual Crossing API integration, detailed feature engineering, and expanded weather columns in ML pipeline.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-advanced-features/20-RESEARCH.md

# Current feature pipeline
@packages/backend/src/lineupiq/features/pipeline.py
@packages/backend/src/lineupiq/data/processing.py

# Existing weather normalization (basic temp/wind)
@packages/backend/src/lineupiq/data/cleaning.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install weather API dependencies and create cached client</name>
  <files>packages/backend/pyproject.toml, packages/backend/src/lineupiq/data/weather_cache.py</files>
  <action>
Add dependencies to pyproject.toml:
- requests-cache (SQLite caching for API responses)
- requests-ratelimiter (rate limiting: 10 req/sec, 1000/day for Visual Crossing free tier)
- python-dotenv (API key management)

Create weather_cache.py with WeatherClient class:
- CachedLimiterSession combining CachedSession + LimiterMixin
- get_game_weather(location, date) method fetching temp, wind, precip, conditions
- SQLite cache with 30-day expiration for historical data
- Rate limiter: 10 requests/second, 1000/day
- Use research code example from 20-RESEARCH.md as template

Environment variable: VISUAL_CROSSING_API_KEY loaded from .env
Base URL: https://weather.visualcrossing.com/VisualCrossingWebServices/rest/services/timeline
  </action>
  <verify>uv sync succeeds, import lineupiq.data.weather_cache works, WeatherClient class exists</verify>
  <done>Dependencies installed, WeatherClient class created with caching and rate limiting</done>
</task>

<task type="auto">
  <name>Task 2: Create detailed weather feature engineering module</name>
  <files>packages/backend/src/lineupiq/features/weather.py</files>
  <action>
Create weather.py module with engineer_weather_features(schedule_df, weather_df) function:

Temperature bins (research-backed thresholds from 20-RESEARCH.md):
- extreme_cold: temp < 25°F (10-15% passing efficiency drop)
- freezing: temp < 32°F
- extreme_heat: temp > 85°F
- temp_filled: Use 72°F for dome games, mean() for missing outdoor temps

Wind thresholds (research: 15mph affects passing):
- high_wind: wind >= 15mph (6% completion rate drop)
- very_high_wind: wind >= 20mph
- wind_filled: 0 for dome games, mean() for missing outdoor wind

Precipitation:
- has_precip: precipitation > 0
- precip_amount: actual precipitation value

Dome/outdoor encoding:
- is_dome: 1 for dome, 0 for outdoor (already exists, keep for consistency)

Use polars for joins and feature creation. Handle dome games by checking roof column from nflreadpy schedules.
DO NOT fetch weather for dome games (waste of API calls). Set dome games to neutral values (72°F, 0 wind, no precip).
  </action>
  <verify>pytest tests/test_weather_features.py passes, function returns expected columns</verify>
  <done>Weather feature engineering creates 9 new columns: extreme_cold, freezing, extreme_heat, temp_filled, high_wind, very_high_wind, wind_filled, has_precip, precip_amount</done>
</task>

<task type="auto">
  <name>Task 3: Integrate detailed weather features into pipeline</name>
  <files>packages/backend/src/lineupiq/features/pipeline.py, packages/backend/tests/test_weather_features.py</files>
  <action>
Update build_features() in pipeline.py:
- Import engineer_weather_features from lineupiq.features.weather
- After loading schedules in Step 4, check if VISUAL_CROSSING_API_KEY exists
- If key exists: fetch weather for outdoor games using WeatherClient, call engineer_weather_features()
- If key missing: log warning, skip detailed weather (graceful degradation for users without API key)
- Join detailed weather features to main dataframe

Update get_feature_columns() to include new weather features in weather_features list:
- Add: extreme_cold, freezing, extreme_heat, high_wind, very_high_wind, has_precip, precip_amount (7 new features)
- Keep existing: temp_normalized, wind_normalized, is_dome (3 existing)
- Total: 10 weather features

Create tests/test_weather_features.py:
- Test engineer_weather_features() creates expected columns
- Test dome games have neutral weather values (72°F, 0 wind, no precip)
- Test outdoor games with missing weather get mean-filled values
- Test temperature/wind bins work correctly (freezing flag for 31°F, high_wind for 16mph, etc.)
- Test precipitation detection (has_precip=1 when precip>0)

Run pytest tests/test_weather_features.py to verify all tests pass.
  </action>
  <verify>pytest tests/test_weather_features.py passes, build_features() includes detailed weather columns, get_feature_columns() returns 10 weather features</verify>
  <done>Weather features integrated into pipeline with 7 new columns, tests passing, graceful degradation without API key</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] uv sync installs requests-cache, requests-ratelimiter, python-dotenv
- [ ] WeatherClient class exists with caching and rate limiting
- [ ] engineer_weather_features() creates 9 weather columns
- [ ] build_features() integrates detailed weather features
- [ ] get_feature_columns() returns 10 weather features total
- [ ] pytest tests/test_weather_features.py passes (all new tests)
- [ ] No errors when VISUAL_CROSSING_API_KEY missing (graceful degradation)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Detailed weather features available for model training (7 new + 3 existing = 10 total)
- Visual Crossing API integration working with caching and rate limiting
- Dome games handled correctly (no API calls, neutral values)
- Tests validate feature engineering logic
  </success_criteria>

<output>
After completion, create `.planning/phases/20-advanced-features/20-01-SUMMARY.md`
</output>
