---
phase: 20-advanced-features
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/backend/src/lineupiq/data/odds_cache.py
  - packages/backend/src/lineupiq/features/matchup.py
  - packages/backend/src/lineupiq/features/pipeline.py
  - packages/backend/tests/test_matchup_features.py
  - packages/backend/pyproject.toml
autonomous: true
domain: ml-features
user_setup:
  - service: the-odds-api
    why: "Historical Vegas betting lines (spreads, totals) for NFL games"
    env_vars:
      - name: ODDS_API_KEY
        source: "https://the-odds-api.com/ → Sign up → Dashboard → API Keys"
    account_setup:
      - url: "https://the-odds-api.com/#get-access"
        skip_if: "Already have The Odds API account"
    local_dev:
      - "Free tier: 500 requests/month (sufficient for prototyping)"
      - "Historical data available back to mid-2020"
---

<objective>
Add Vegas lines and matchup-specific features (spreads, totals, home/away, divisional games) to capture market efficiency signals and contextual game factors.

Purpose: Improve prediction accuracy by incorporating betting market consensus (Vegas spreads/totals as proxy for expected team performance) and home field advantage.
Output: Matchup feature module with The Odds API integration, Vegas line features, home/away splits, and divisional game flags in ML pipeline.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-advanced-features/20-RESEARCH.md

# Current feature pipeline
@packages/backend/src/lineupiq/features/pipeline.py
@packages/backend/src/lineupiq/data/fetchers.py

# Schedule data with home/away teams
@packages/backend/src/lineupiq/data/processing.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cached Odds API client</name>
  <files>packages/backend/src/lineupiq/data/odds_cache.py</files>
  <action>
Create odds_cache.py with OddsClient class (requests-cache and requests-ratelimiter already installed from 20-01):

OddsClient methods:
- __init__(api_key, cache_name='odds_cache'): CachedSession with 7-day expiration (betting lines shift)
- get_historical_odds(date: str): Fetch NFL odds for specific date (YYYY-MM-DD format)
  - Base URL: https://api.the-odds-api.com/v4/sports/americanfootball_nfl/odds
  - Params: apiKey, regions=us, markets=spreads,totals, dateFormat=iso, date={date}
  - Returns list of games with bookmaker odds
- parse_odds(games_json): Extract average spreads and totals across bookmakers
  - For spreads: average home_spread across all bookmakers
  - For totals: average total_points across all bookmakers
  - Returns polars DataFrame with game_id, home_team, away_team, home_spread, total_points

Environment variable: ODDS_API_KEY loaded from .env
Cache: SQLite backend, 7-day expiration (lines can shift during week)
No rate limiter needed (free tier: 500/month is low enough to manually track)

Use research code example from 20-RESEARCH.md as template.
  </action>
  <verify>import lineupiq.data.odds_cache works, OddsClient class exists with get_historical_odds() and parse_odds() methods</verify>
  <done>OddsClient class created with caching, historical odds fetching, and parsing logic</done>
</task>

<task type="auto">
  <name>Task 2: Create matchup feature engineering module</name>
  <files>packages/backend/src/lineupiq/features/matchup.py, packages/backend/tests/test_matchup_features.py</files>
  <action>
Create matchup.py module with engineer_matchup_features(schedule_df, odds_df) function:

Vegas line features (from odds_df):
- home_spread: averaged spread across bookmakers (negative = home favored)
- total_points: averaged over/under total
- vegas_strength_diff: abs(home_spread) as proxy for game competitiveness
- home_favored: binary flag (1 if home_spread < 0, 0 otherwise)

Home/away features (from schedule_df):
- is_home: binary flag (already exists in pipeline, keep for consistency)
- home_field_advantage: calculated from research (average +2.5-3 points for home team)
  Note: Research shows team-specific HFA varies (+6.4 for Detroit, -1.7 for Washington)
  For v1: use simple binary is_home flag, defer team-specific HFA to future phase

Divisional game features (from schedule_df):
- is_divisional: binary flag (1 if opponent in same division, 0 otherwise)
  Check: both teams share division (NFC East, AFC North, etc.)
  Use team division mapping from nflreadpy or hardcode 8 divisions

Implementation:
- Join odds_df to schedule_df on game_id
- Handle missing odds (pre-2020 games have no Vegas data): fill_null with neutral values
  - home_spread: 0.0 (neutral, no favorite)
  - total_points: 45.0 (approximate NFL average)
  - vegas_strength_diff: 0.0
  - home_favored: 0
- Calculate divisional flag from team divisions
- Return schedule_df with added matchup features

Create tests/test_matchup_features.py:
- Test Vegas line join (home_spread, total_points added correctly)
- Test home_favored flag (1 when spread<0, 0 otherwise)
- Test vegas_strength_diff (abs value of spread)
- Test missing odds handling (pre-2020 games get neutral fills)
- Test is_divisional flag (NFC East teams playing each other = 1)

Use polars for joins. Follow research code example from 20-RESEARCH.md.
  </action>
  <verify>pytest tests/test_matchup_features.py passes, function creates Vegas and divisional features</verify>
  <done>Matchup feature engineering creates 5 new columns: home_spread, total_points, vegas_strength_diff, home_favored, is_divisional</done>
</task>

<task type="auto">
  <name>Task 3: Integrate matchup features into pipeline</name>
  <files>packages/backend/src/lineupiq/features/pipeline.py</files>
  <action>
Update build_features() in pipeline.py:
- Import OddsClient from lineupiq.data.odds_cache
- Import engineer_matchup_features from lineupiq.features.matchup
- After Step 6 (injury features), add Step 7: matchup features
- Check if ODDS_API_KEY exists (similar to weather API key check)
- If key exists:
  - Fetch unique game dates from schedules_df
  - For each date, call odds_client.get_historical_odds(date)
  - Parse odds into DataFrame with odds_client.parse_odds()
  - Call engineer_matchup_features(schedules_df, odds_df)
  - Join matchup features to main player DataFrame on [season, week, team]
- If key missing: log warning, skip Vegas features but still add is_divisional flag
  (divisional flag doesn't require external API, only schedule data)

Update get_feature_columns() to include matchup features:
- Add to new matchup_features list: ["home_spread", "total_points", "vegas_strength_diff", "home_favored", "is_divisional"]
- Include in final return statement (after injury features)

Update docstring for build_features():
- Document that matchup features are included (Step 7)
- Note research: Vegas lines as market efficiency signal, HFA averages +2.5-3 pts
- Add matchup columns to return value documentation

Handle API key gracefully:
- If ODDS_API_KEY missing: create matchup features without Vegas lines (only divisional flag)
- Log clear message: "ODDS_API_KEY not found, skipping Vegas features. Set in .env for spreads/totals."

Run pytest tests/test_feature_pipeline.py to ensure integration doesn't break existing tests.
  </action>
  <verify>pytest tests/test_feature_pipeline.py passes, build_features() includes matchup columns, get_feature_columns() returns matchup features</verify>
  <done>Matchup features integrated into pipeline with 5 new columns (or 1 if no API key), tests passing, graceful degradation without API key</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] OddsClient class exists in data/odds_cache.py
- [ ] engineer_matchup_features() creates Vegas and divisional features
- [ ] build_features() includes Step 7 for matchup features
- [ ] get_feature_columns() returns matchup features (home_spread, total_points, vegas_strength_diff, home_favored, is_divisional)
- [ ] pytest tests/test_matchup_features.py passes (all new tests)
- [ ] pytest tests/test_feature_pipeline.py passes (integration doesn't break existing)
- [ ] Graceful degradation when ODDS_API_KEY missing (divisional flag still works)
- [ ] Pre-2020 games handle missing Vegas data with neutral fills
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Matchup features available for model training (5 new columns)
- The Odds API integration working with caching
- Vegas lines (spreads, totals) added for 2020+ games
- Divisional game flag works for all seasons
- Missing API key doesn't break pipeline (graceful degradation)
- Tests validate feature engineering and Vegas line parsing
  </success_criteria>

<output>
After completion, create `.planning/phases/20-advanced-features/20-03-SUMMARY.md`
</output>
