---
phase: 19.2-improve-model-confidence-accuracy
plan: 03
type: execute
wave: 3
depends_on: ["19.2-02"]
files_modified:
  - packages/frontend/components/matchup/ModelConfidence.tsx
autonomous: true
---

<objective>
Update UI to reflect the improved accuracy metric and validate that models now display accurate confidence levels.

Purpose: Ensure users see intuitive accuracy percentages that match model performance (e.g., 55% for R² 0.545 instead of 47%).
Output: Updated ModelConfidence component with improved tooltips and validation that accuracy displays correctly.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.2-improve-model-confidence-accuracy/19.2-01-SUMMARY.md
@.planning/phases/19.2-improve-model-confidence-accuracy/19.2-02-SUMMARY.md
@packages/frontend/components/matchup/ModelConfidence.tsx
@packages/backend/src/lineupiq/models/accuracy.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ModelConfidence tooltip to explain R²-based accuracy</name>
  <files>packages/frontend/components/matchup/ModelConfidence.tsx</files>
  <action>
    Update the tooltip title attribute in ModelConfidence.tsx to explain the new metric:

    **Current tooltip:**
    ```tsx
    title={`Model accuracy: ${accuracyPct.toFixed(1)}%`}
    ```

    **New tooltip:**
    ```tsx
    title={`Model accuracy: ${accuracyPct.toFixed(1)}% (variance explained by the model)`}
    ```

    **Rationale:**
    - Explains what accuracy percentage means in ML terms
    - "Variance explained" is more accurate than previous interpretation
    - Helps users understand that 55% is good for regression models

    **Optional enhancement (if time permits):**
    Add a small info icon with popover explaining:
    - High confidence (>50%): Model explains majority of variance
    - Medium confidence (30-50%): Model captures key trends
    - Low confidence (<30%): Model has limited predictive power

    Keep this simple for now - just update the tooltip. Popover can be added later if user feedback requests it.
  </action>
  <verify>Tooltip shows "variance explained by the model" text</verify>
  <done>Tooltip updated to explain R²-based accuracy</done>
</task>

<task type="auto">
  <name>Task 2: Verify accuracy displays correctly in dev environment</name>
  <files>None (verification only)</files>
  <action>
    Test the updated accuracy display in the Next.js dev server:

    1. Start API server:
    ```bash
    cd packages/backend
    uv run uvicorn src.lineupiq.api.main:app --reload
    ```

    2. Start frontend dev server:
    ```bash
    cd packages/frontend
    pnpm dev
    ```

    3. Navigate to matchup page (http://localhost:3000/matchup)

    4. Select a player and opponent, submit prediction

    5. Verify ModelConfidence component shows:
       - Accuracy percentage matches expected R²-based value
       - For models with R² ~0.5, should show ~50% accuracy (not ~47%)
       - Confidence badge reflects correct tier (High for R² > 0.5)

    6. Test multiple positions (QB, RB, WR, TE) to verify consistency

    **Expected behavior:**
    - QB passing_yards (R² 0.524) → ~52% accuracy, Medium confidence
    - RB rushing_yards (R² 0.545) → ~55% accuracy, Medium confidence
    - RB carries (R² 0.696) → ~70% accuracy, High confidence

    **What to check:**
    - No console errors
    - Accuracy percentages align with benchmark R² values
    - Confidence tiers make sense (50-70% shows as Medium/High, not Low)
  </action>
  <verify>Dev environment shows R²-based accuracy matching model performance</verify>
  <done>Verified accuracy displays correctly for multiple positions</done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md with accuracy metric fix</name>
  <files>.planning/STATE.md</files>
  <action>
    Add Phase 19.2 decision to STATE.md accumulated decisions:

    ```markdown
    | 19.2-02 | R²-based accuracy metric replaces MAE/mean formula | Standard ML metric; accuracy_pct = 100*R² aligns with model performance |
    | 19.2-02 | Confidence tiers based on R² only | Simplified logic: High (R²>0.5), Medium (0.3-0.5), Low (<0.3) |
    ```

    **Remove or mark as superseded:**
    ```markdown
    | 12 | Accuracy % formula: 100*(1-MAE/mean) | ~~Intuitive 0-100% scale for user trust~~ → SUPERSEDED by 19.2-02 |
    ```

    **Update Current Position in STATE.md:**
    - Phase: 19.2 → 20 (ready for next phase)
    - Status: Complete
    - Last activity: 2026-01-20 - Phase 19.2 complete, accuracy metric fixed

    This documents the architectural decision change and makes it easy to understand why the formula changed.
  </action>
  <verify>STATE.md shows Phase 19.2 decisions and marks old formula as superseded</verify>
  <done>STATE.md updated with accuracy metric fix documentation</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ModelConfidence tooltip explains "variance explained"
- [ ] Dev environment tested with multiple positions
- [ ] Accuracy percentages align with R² values from benchmark
- [ ] STATE.md updated with Phase 19.2 decisions
- [ ] No console errors or broken UI
</verification>

<success_criteria>

- Tooltip updated to explain R²-based accuracy
- Manual verification confirms accuracy displays correctly
- Models with R² 0.545 show ~55% accuracy (not 47%)
- Confidence tiers match expectations (High for R² > 0.5)
- STATE.md documents the metric change
  </success_criteria>

<output>
After completion, create `.planning/phases/19.2-improve-model-confidence-accuracy/19.2-03-SUMMARY.md`
</output>
