---
phase: 21-position-tuning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [models/, models_archive/, .planning/phases/21-position-tuning/21-BASELINE.md, training.log]
autonomous: true
---

<objective>
Retrain all 32 models with expanded 40-feature set from Phase 20, establishing baseline performance metrics for position-specific tuning.

Purpose: Phase 20 added 14 new ML features (weather, matchup). Models need retraining to incorporate these signals. Injury features excluded from training (prediction-time only). This plan establishes the baseline before position-specific hyperparameter optimization.

Output: Freshly trained models in models/, archived pre-Phase 21 models, documented baseline metrics in 21-BASELINE.md showing R²/MAE/RMSE per model with expanded features.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 20 context - expanded feature set
@.planning/phases/20-advanced-features/20-01-SUMMARY.md
@.planning/phases/20-advanced-features/20-02-SUMMARY.md
@.planning/phases/20-advanced-features/20-03-SUMMARY.md

# Training infrastructure
@packages/backend/scripts/train_all.py
@packages/backend/TRAINING.md

# Feature pipeline with 40 features
@packages/backend/src/lineupiq/features/pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive pre-Phase 21 models</name>
  <files>models/, models_archive/phase20_pre_tuning/</files>
  <action>
    Create archive directory: `mkdir -p models_archive/phase20_pre_tuning`

    Copy all existing models to archive:
    `cp models/*.joblib models_archive/phase20_pre_tuning/`

    Count archived models:
    `ls models_archive/phase20_pre_tuning/*.joblib | wc -l`

    This preserves pre-Phase 21 models trained with 28-feature set (before weather/injury/matchup features) for comparison and rollback if needed.
  </action>
  <verify>
    - `ls models_archive/phase20_pre_tuning/*.joblib | wc -l` shows 21+ models archived
    - Original models/ directory still contains .joblib files (we copy, not move)
  </verify>
  <done>All existing models backed up to models_archive/phase20_pre_tuning/ with .joblib files preserved</done>
</task>

<task type="auto">
  <name>Task 2: Retrain all models with expanded feature set</name>
  <files>models/*.joblib, training.log</files>
  <action>
    Run full training pipeline with expanded 40-feature set:
    `uv run python scripts/train_all.py --seasons 2022 2023 2024 2025 --trials 30 --rolling-window 5`

    This trains all 32 models (6 QB + 7 RB + 4 WR + 4 TE + 5 K + 6 DEF) with:
    - 40 ML features (Phase 20 added 14: 7 weather, 5 matchup, 2 team updates; injury excluded)
    - 2022-2025 training data (4-year window from Phase 19.1)
    - 5-game rolling window (Phase 19.1 improvement)
    - 30 Optuna trials per model (standardized from Phase 13)
    - LightGBM default (7x faster than XGBoost per Phase 13)

    Expected duration: ~45-60 minutes for all positions.

    Training outputs R²/MAE/RMSE metrics to training.log and saves models to models/ directory.

    Key: This is baseline retraining with expanded features. Injury features excluded (prediction-time only per Phase 21 context). Position-specific tuning (>30 trials, custom feature subsets) deferred to later if baseline shows improvement opportunity.
  </action>
  <verify>
    - `ls models/*.joblib | wc -l` shows 32 models (all positions trained)
    - `tail -100 training.log` shows completion messages for all positions
    - No training errors in training.log
    - Models have recent timestamps: `ls -lt models/*.joblib | head -5`
  </verify>
  <done>All 32 models retrained with 40-feature set, training.log contains complete metrics, models/*.joblib files updated with timestamps from this run</done>
</task>

<task type="auto">
  <name>Task 3: Document baseline performance metrics</name>
  <files>.planning/phases/21-position-tuning/21-BASELINE.md</files>
  <action>
    Create comprehensive baseline metrics document from training.log:

    Parse training.log for each model's metrics:
    - Model name (e.g., "QB_passing_yards")
    - R² (coefficient of determination, 0.0-1.0)
    - MAE (Mean Absolute Error)
    - RMSE (Root Mean Squared Error)
    - n_samples (training sample count)

    Structure 21-BASELINE.md as:

    ```markdown
    # Phase 21 Baseline Performance

    **Trained:** [date]
    **Feature count:** 40 (Phase 20: +14 from weather/matchup; injury excluded)
    **Training data:** 2022-2025 (4 seasons)
    **Rolling window:** 5 games
    **Optuna trials:** 30 per model
    **Model type:** LightGBM

    ## QB Models (6)
    | Model | R² | MAE | RMSE | Samples |
    |-------|----|----|------|---------|
    | passing_yards | X.XXX | XX.X | XX.X | XXXX |
    ...

    ## RB Models (7)
    ...

    ## WR Models (4)
    ...

    ## TE Models (4)
    ...

    ## K Models (5)
    ...

    ## DEF Models (6)
    ...

    ## Summary Statistics
    - **Average R² across all models:** X.XXX
    - **Models with R² > 0.5:** XX/32 (XX%)
    - **Models with R² > 0.3:** XX/32 (XX%)
    - **Lowest R²:** [model] (X.XXX)
    - **Highest R²:** [model] (X.XXX)

    ## Feature Set Comparison

    Phase 19.2.1 (28 features):
    - Rolling stats (8), opponent strength (5), team strength (3), volatility (8), weather (2), home/dome (2)

    Phase 21 (40 features):
    - **Added:** extreme_cold, freezing, extreme_heat, high_wind, very_high_wind, has_precip, precip_amount (weather)
    - **Added:** home_spread, total_points, vegas_strength_diff, home_favored, is_divisional (matchup)
    - **Updated:** team_points_roll5, team_yards_roll5 (now 5-game window)
    - **Excluded:** injury features (prediction-time only, not used in training)

    ## Next Steps

    If baseline shows room for improvement (low R² models, high error stats):
    - Position-specific feature selection (not all 40 features relevant to all positions)
    - Increased Optuna trials for low-performing models (50-100 trials vs current 30)
    - Position-specific hyperparameter ranges (QB passing vs RB rushing have different optimal trees/depth)

    If baseline shows strong performance (R² > 0.5 for most models):
    - Models ready for production use
    - Focus shifts to UI/UX improvements in later phases
    ```

    Extract metrics programmatically by grepping training.log for pattern matches:
    - "Training [Position] models" (section markers)
    - "Best trial: R²=X.XXX, MAE=XX.X, RMSE=XX.X" (metrics)
    - "n_samples: XXXX" (sample counts)

    Calculate summary statistics (averages, counts, min/max R²).

    This document serves as decision point: proceed with position-specific tuning if baseline shows opportunity, or ship models if performance is strong.
  </action>
  <verify>
    - `.planning/phases/21-position-tuning/21-BASELINE.md` exists
    - Document contains metrics for all 32 models
    - Summary statistics calculated (avg R², distribution counts)
    - Feature comparison table shows Phase 20 additions
  </verify>
  <done>21-BASELINE.md created with complete metrics table for all 32 models, summary statistics, feature set comparison, and next steps guidance</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] models_archive/phase20_pre_tuning/ contains backed up models
- [ ] models/ directory contains 32 freshly trained .joblib files
- [ ] training.log shows successful completion of all positions
- [ ] 21-BASELINE.md exists with complete metrics for all 32 models
- [ ] No training errors or failures in training.log
- [ ] Model timestamps confirm recent training (today's date)
</verification>

<success_criteria>

- All tasks completed
- 32 models retrained with 40-feature set (Phase 20 additions, injury excluded)
- Pre-Phase 21 models archived for rollback capability
- Baseline metrics documented in 21-BASELINE.md
- No training errors or convergence issues
- training.log shows R²/MAE/RMSE for all models
- Decision data available for position-specific tuning (later phases if needed)
</success_criteria>

<output>
After completion, create `.planning/phases/21-position-tuning/21-01-SUMMARY.md`
</output>
