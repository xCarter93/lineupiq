---
phase: 16-ui-data-visualization
plan: 02
task: 2
total_tasks: 2
status: blocked
last_updated: 2026-01-15
---

<current_state>
Phase 16 is partially complete. Plan 16-01 (Recharts Setup) finished successfully. Plan 16-02 (Chart Integration) has Task 1 done but is blocked on Task 2 (human-verify checkpoint) due to a data dependency issue.

The FantasyPointsChart component is integrated and the code is correct, BUT the chart doesn't display because the sample seed players use slug-format IDs (`hurts-jalen`) while the player history API expects real `gsis_id` format (`00-0033873`).
</current_state>

<completed_work>
**Phase 16:**
- Plan 16-01: Complete (3/3 tasks)
  - Recharts 3.6.0 installed
  - FantasyPointsChart component created
  - Dynamic import wrappers with SSR-safe loading
  - Commits: e184185, 3c8daf6, 2370c90, 4de4338

- Plan 16-02: Task 1 complete
  - FantasyPointsChart integrated into PlayerHistory component
  - Chart appears in SeasonSummary when 2+ data points exist
  - Commit: b5fcb37

**Bug fixes discovered during execution:**
- Fixed validation API 500 error (sample_count key mismatch) - commit 9357876
- Fixed prediction API 500 error (missing 11 features in schema) - commit a8d95a9
  - Added team_points_roll3, team_yards_roll3, team_plays_roll3
  - Added 8 volatility features (*_std3, *_cv3)
</completed_work>

<remaining_work>
- Plan 16-02 Task 2: Human verification of chart
  - Chart code is correct but can't verify because sample players lack real gsis_ids
  - **Options to unblock:**
    1. User syncs roster via Admin page (/admin → "Sync 2025 Roster")
    2. Update seed players with real gsis_ids

- After 16-02 completes:
  - Update ROADMAP.md to mark Phase 16 complete
  - Plan/execute Phase 17 (Model Explainability UI)
</remaining_work>

<decisions_made>
- 16-01: Direct recharts install (not shadcn/ui chart) - Tailwind v4 compatibility
- 16-01: Added `module: esnext` to tsconfig.base.json for dynamic imports
- 16-02: Chart only shows when chartData has >1 data point (line needs 2+ points)
- Bug fix: Added 11 missing features to PredictionRequest schema to match 28-feature model
</decisions_made>

<blockers>
- **Chart verification blocked**: Sample seed players in `seedPlayers.ts` use slug IDs (`hurts-jalen`) but the `/api/player/{player_id}/history` endpoint expects real `gsis_id` format.
  - **Workaround**: User can sync roster from Admin page to get real player IDs
  - **Alternative fix**: Update SAMPLE_PLAYERS with real gsis_ids (haven't implemented)
</blockers>

<context>
The chart integration code (16-02 Task 1) is complete and correct. The issue is a pre-existing data mismatch:
- Sample seed players were created for UI testing with made-up IDs
- Player history API requires real NFL gsis_ids to fetch from nflreadpy

The proper flow is:
1. Admin page → "Sync 2025 Roster" → imports ~500 real players with gsis_ids
2. Then player history API works
3. Then charts display

The verification was paused waiting for user to sync roster or confirm they want me to update the seed data.
</context>

<next_action>
Resume with: Ask user if they synced roster. If yes, complete 16-02 verification. If no, offer to either:
1. Have them sync roster via /admin
2. Update seed players with real gsis_ids
3. Mark 16-02 as complete since code is correct (data dependency is separate issue)
</next_action>
