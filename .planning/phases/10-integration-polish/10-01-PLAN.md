---
phase: 10-integration-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/app/page.tsx
  - packages/frontend/components/layout/Header.tsx
  - packages/frontend/app/layout.tsx
autonomous: true
---

<objective>
Update homepage from placeholder to proper landing page with navigation to matchup simulator.

Purpose: Create cohesive user experience with clear entry point to the matchup tool.
Output: Landing page with hero section and header with navigation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-matchup-ui/09-01-SUMMARY.md

# Source files for patterns
@packages/frontend/app/page.tsx
@packages/frontend/app/matchup/page.tsx
@packages/frontend/app/globals.css
@packages/frontend/components/ui/section-label.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Header component with navigation</name>
  <files>packages/frontend/components/layout/Header.tsx</files>
  <action>
    Create Header component with:
    - LineupIQ logo/text on the left
    - Navigation link to /matchup on the right
    - Follow Greptile design: clean white background with subtle shadow, warm cream body
    - Use Link from next/link for navigation
    - Responsive: logo centered on mobile, full layout on desktop
    - Export from components/layout/Header.tsx
  </action>
  <verify>npx tsc --noEmit passes</verify>
  <done>Header component renders with navigation link to matchup page</done>
</task>

<task type="auto">
  <name>Task 2: Update layout to include Header</name>
  <files>packages/frontend/app/layout.tsx</files>
  <action>
    Import and add Header component to layout:
    - Import Header from @/components/layout/Header
    - Place Header above {children} inside body
    - Keep existing ConvexClientProvider wrapping
    - Body should maintain warm cream background from globals.css
    - DO NOT add footer - keep simple
  </action>
  <verify>pnpm build in frontend passes</verify>
  <done>Header appears on all pages including matchup page</done>
</task>

<task type="auto">
  <name>Task 3: Redesign homepage as landing page</name>
  <files>packages/frontend/app/page.tsx</files>
  <action>
    Transform placeholder into landing page:
    - Hero section with LineupIQ branding and tagline ("ML-powered fantasy football predictions")
    - Brief value proposition (3-4 bullet points max)
    - Primary CTA button linking to /matchup ("Build a Matchup" or "Get Started")
    - Follow Greptile design patterns: warm cream background, white card for content
    - Use existing SectionLabel component for section headers
    - Keep simple - no feature comparisons or pricing
    - Use Link from next/link for CTA (NOT Button with href)
  </action>
  <verify>Visit http://localhost:3000 and verify landing page renders correctly</verify>
  <done>Homepage shows landing content with clear CTA to matchup page</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build` in packages/frontend succeeds
- [ ] Homepage displays landing page content
- [ ] Header appears on both homepage and matchup page
- [ ] Navigation from homepage to /matchup works
- [ ] Back navigation or clicking logo returns to homepage
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- User can navigate from homepage to matchup and back
- Consistent Greptile-inspired design across pages
</success_criteria>

<output>
After completion, create `.planning/phases/10-integration-polish/10-01-SUMMARY.md`
</output>
