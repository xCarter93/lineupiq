---
phase: 18-performance-fixes-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/components/matchup/PlayerSelect.tsx
  - packages/frontend/package.json
autonomous: true
---

<objective>
Virtualize the player dropdown to eliminate lag when opening/searching with 500+ players.

Purpose: Fix the primary performance bottleneck - rendering all player options at once causes 2-5 second lag when dropdown opens.
Output: Smooth, instant player selection using react-window virtualization.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-performance-fixes-optimization/18-RESEARCH.md
@packages/frontend/components/matchup/PlayerSelect.tsx
@packages/frontend/components/ui/combobox.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-window dependencies</name>
  <files>packages/frontend/package.json</files>
  <action>Install react-window (1.8.10) and @types/react-window for TypeScript support. Use pnpm add from the frontend package directory. Do NOT install react-window-infinite-loader (not needed for this phase - search filtering is client-side).</action>
  <verify>pnpm list react-window shows version 1.8.10, @types/react-window installed</verify>
  <done>Dependencies installed and available for import</done>
</task>

<task type="auto">
  <name>Task 2: Create VirtualizedPlayerSelect component</name>
  <files>packages/frontend/components/matchup/PlayerSelect.tsx</files>
  <action>Replace the current ComboboxList implementation with react-window FixedSizeList. Key implementation details from RESEARCH.md:

  - Import FixedSizeList from 'react-window'
  - Create custom MenuList component that wraps FixedSizeList
  - Item height: 48px (current player item with avatar is ~48px tall)
  - Visible height: min(maxHeight, filteredPlayers.length * 48) to avoid empty space
  - Width: 100% to fill combobox container
  - Render filteredPlayers[index] in each row with same ComboboxItem structure (avatar, name, team badge)
  - Keep existing search filtering logic (useMemo with searchQuery) - virtualization applies AFTER filtering
  - Disable accent stripping if using createFilter (performance optimization from RESEARCH.md)

  IMPORTANT: Only virtualize the list rendering, keep all other functionality (search, selection, avatars, team badges) exactly as-is. The user should see no difference except the performance improvement.</action>
  <verify>Player dropdown opens instantly, scrolling is smooth, search still works, selected player displays correctly</verify>
  <done>Dropdown renders only ~10 visible items instead of all 500+, selection and search work identically to before</done>
</task>

<task type="auto">
  <name>Task 3: Test virtualized player selection</name>
  <files>None (testing)</files>
  <action>Start dev server (pnpm dev from root or packages/frontend). Navigate to /matchup page. Test:

  1. Click player dropdown - should open instantly (no lag)
  2. Scroll through list - should be smooth
  3. Search for "Mahomes" - should filter and highlight result
  4. Select player - should populate form correctly
  5. Change position filter - dropdown should update and still perform well
  6. Check console for errors or warnings

  If dropdown shows empty or doesn't scroll: check item height matches actual rendered height. If search breaks: verify filtering happens before virtualization. If selection fails: ensure ComboboxItem value prop is correct.</action>
  <verify>All player selection flows work with instant dropdown open and smooth scrolling</verify>
  <done>Player selection is visibly faster, no regressions in functionality</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm dev runs without errors
- [ ] Player dropdown opens instantly (< 100ms perceived)
- [ ] Scrolling through 500+ players is smooth
- [ ] Search filtering still works
- [ ] Player selection populates form correctly
- [ ] No console errors or warnings
</verification>

<success_criteria>

- react-window installed and imported successfully
- PlayerSelect component uses FixedSizeList virtualization
- Dropdown lag eliminated (opens instantly)
- All existing functionality preserved (search, selection, avatars, badges)
- No visual or functional regressions
  </success_criteria>

<output>
After completion, create `.planning/phases/18-performance-fixes-optimization/18-01-SUMMARY.md`
</output>
