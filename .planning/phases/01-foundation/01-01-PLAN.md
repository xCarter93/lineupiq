---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pnpm-workspace.yaml, package.json, .gitignore, .nvmrc, .prettierrc, tsconfig.base.json]
autonomous: true
---

<objective>
Create monorepo root structure with pnpm workspaces configuration.

Purpose: Establish the foundation for a polyglot monorepo containing Python ML backend and Next.js frontend.
Output: Working pnpm workspace configuration with shared tooling.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pnpm workspace configuration</name>
  <files>pnpm-workspace.yaml, package.json</files>
  <action>
    Create pnpm-workspace.yaml defining workspace packages:
    - packages/frontend (Next.js app)
    - packages/backend will exist but is Python/uv managed, not pnpm

    Create root package.json with:
    - name: "lineupiq"
    - private: true
    - packageManager: "pnpm@9.15.4" (or latest stable)
    - scripts for workspace commands (dev, build, lint)
    - No dependencies at root level yet
  </action>
  <verify>pnpm install succeeds (creates pnpm-lock.yaml)</verify>
  <done>pnpm-workspace.yaml exists, root package.json has workspace config</done>
</task>

<task type="auto">
  <name>Task 2: Create shared development configuration</name>
  <files>.gitignore, .nvmrc, .prettierrc, tsconfig.base.json</files>
  <action>
    Create .gitignore with patterns for:
    - Node: node_modules, .pnpm-store, dist, .next, out
    - Python: __pycache__, .venv, *.pyc, .pytest_cache, .mypy_cache
    - IDE: .idea, .vscode (except settings.json), *.swp
    - OS: .DS_Store, Thumbs.db
    - Env: .env, .env.local, .env*.local

    Create .nvmrc with Node version: 20

    Create .prettierrc with:
    - semi: true
    - singleQuote: true
    - tabWidth: 2
    - trailingComma: "es5"
    - printWidth: 100

    Create tsconfig.base.json with:
    - target: "ES2022"
    - lib: ["dom", "dom.iterable", "esnext"]
    - strict: true
    - skipLibCheck: true
    - moduleResolution: "bundler"
    - resolveJsonModule: true
    - esModuleInterop: true
    - This will be extended by frontend package
  </action>
  <verify>Files exist and are valid JSON/YAML where applicable</verify>
  <done>.gitignore, .nvmrc, .prettierrc, tsconfig.base.json created with correct content</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm-workspace.yaml exists with correct structure
- [ ] Root package.json is valid with workspace scripts
- [ ] pnpm install completes without errors
- [ ] .gitignore covers Node, Python, IDE patterns
- [ ] .nvmrc specifies Node 20
- [ ] .prettierrc and tsconfig.base.json are valid JSON
</verification>

<success_criteria>

- All tasks completed
- pnpm workspace ready for frontend package
- Shared configs in place for TypeScript projects
- Git tracking correct files (ignoring build artifacts, env files)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
