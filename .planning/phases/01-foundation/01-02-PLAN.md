---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [packages/backend/pyproject.toml, packages/backend/src/lineupiq/__init__.py, packages/backend/README.md]
autonomous: true
---

<objective>
Set up Python ML backend with uv package manager.

Purpose: Create the Python environment for ML model training, data processing, and prediction API.
Output: Working Python package with uv, core ML dependencies, and project structure.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Python backend structure with uv</name>
  <files>packages/backend/pyproject.toml, packages/backend/src/lineupiq/__init__.py</files>
  <action>
    Create packages/backend/ directory structure:
    ```
    packages/backend/
    ├── pyproject.toml
    ├── src/
    │   └── lineupiq/
    │       └── __init__.py
    └── tests/
        └── __init__.py
    ```

    Create pyproject.toml with:
    - name: "lineupiq"
    - version: "0.1.0"
    - requires-python: ">=3.11"
    - Build system: hatchling
    - Core dependencies:
      - nflreadpy (NFL data)
      - pandas, numpy (data processing)
      - scikit-learn (ML utilities)
      - xgboost (ensemble models)
      - fastapi, uvicorn (API server - for Phase 7)
    - Dev dependencies:
      - pytest, pytest-cov
      - ruff (linting)
      - mypy (type checking)

    Create src/lineupiq/__init__.py with:
    - __version__ = "0.1.0"
    - Docstring describing the package

    Initialize uv and create virtual environment.
  </action>
  <verify>cd packages/backend && uv sync && uv run python -c "import lineupiq; print(lineupiq.__version__)"</verify>
  <done>Python package installs successfully, imports work</done>
</task>

<task type="auto">
  <name>Task 2: Verify nflreadpy installation and basic data access</name>
  <files>packages/backend/src/lineupiq/__init__.py</files>
  <action>
    After uv sync, verify nflreadpy works by running a quick test:
    - Import nflreadpy
    - Call a lightweight function to confirm installation

    Note: Do NOT download large datasets here - just verify the package works.
    The actual data pipeline is Phase 2.

    Add a simple health check function to __init__.py:
    ```python
    def check_nflreadpy() -> bool:
        """Verify nflreadpy is installed and importable."""
        try:
            import nflreadpy
            return True
        except ImportError:
            return False
    ```
  </action>
  <verify>cd packages/backend && uv run python -c "from lineupiq import check_nflreadpy; assert check_nflreadpy()"</verify>
  <done>nflreadpy imports successfully, health check function works</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] packages/backend/pyproject.toml exists with correct dependencies
- [ ] uv sync completes without errors
- [ ] Python package is importable
- [ ] nflreadpy imports successfully
- [ ] Project structure follows Python best practices (src layout)
</verification>

<success_criteria>

- All tasks completed
- Python environment working with uv
- Core ML and data dependencies installed
- nflreadpy accessible for Phase 2 data pipeline work
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
